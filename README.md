# AI GIF Animator (Coursework Submission)

This application was automatically generated by an AI-powered meta-software.

**No configuration needed**: The API key for LLM access is already embedded in the code. Just run:
pip install -r requirements.txt
python main.py

Then open http://127.0.0.1:5005

The homepage displays a default animated GIF. You can upload your own image and enter an action description to create custom GIFs.
